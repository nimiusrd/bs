<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>$title$</title>
  $for(author)$<meta name="author" content="$author$">$endfor$
  $for(date)$<meta name="date" content="$date$">$endfor$
  $if(css)$
    $for(css)$<link rel="stylesheet" href="$css$">$endfor$
  $endif$
</head>
<body>
  <header>
    <h1 class="page-title">$title$</h1>
    $if(toc)$
    <nav class="toc-container">
      <h2>目次</h2>
      $toc$
    </nav>
    $endif$
  </header>
  <main>
    $body$
  </main>
</body>
</html>

